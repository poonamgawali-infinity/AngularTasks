import { Data } from './data-model';

export class TextBoxQuestion extends Data{
 // controlType = 'string';
  //type: string;

  constructor(options: {} = {}) {
    super(options);
 //  this.type = options['type'] || '';
  }
}